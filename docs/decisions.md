# config.py

- FastAPI over Flask
- FastAPI has native Pydantic v2 integration (used for LLM output validation), automatic OpenAPI docs and 
async support for future streaming
- Flask needed manual JSON validation and has no async story.

# app.py
